TABLE "SFLIGHT_LOG" (
    "CARRID" NVARCHAR(3),
    "CONNID" INTEGER,
    "FLDATE" DATE,
    "PRICE" DECIMAL(15,2),
    "CURRENCY" NVARCHAR(5),
    "SEATSMAX" INTEGER,
    "SEATSOCC" INTEGER,
    "FECHA_CARGA" DATE,
    "LOAD_TIMESTAMP" TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
